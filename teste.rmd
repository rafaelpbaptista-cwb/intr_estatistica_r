## Introdução

Este é um exemplo básico de R Markdown.

## Plot de Gráfico

Vamos plotar um gráfico simples da função seno.

```{r plot}
x <- seq(0, 2*pi, length.out = 100)
y <- sin(x)
plot(x, y, type='l', col='blue')
```